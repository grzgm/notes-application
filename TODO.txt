czy Account potrzebuje AmountOfNotes tak, tam będzie to obliczał
ile DTO do pobierania konta z bazy
                    using (SqlCommand command = new SqlCommand(sql, connection))
                    {
                        connection.Open();
                        using (SqlDataReader reader = command.ExecuteReader())
                        {
                            while (reader.Read())
                            {
                                Console.WriteLine("{0} {1}", reader.GetString(0), reader.GetString(1));
                            }
                        }
                    } 


Notes of right user are displayed
Left side displaying user info
hide acces to content without login or register
getting premium account form web
Relation between notes and account
Turning PremiumUser to User when times comes

changed delete date route id to noteid

when no notes add something to css like if has one child// or if note+ is first child then

combined key for name and email in account

combined key for noteId and userId in notes

LOG OUT